import { createContext } from 'react'

//Excuse me for using any so often. It's just that I still have a hard time prescribing types and I don't have any practice.
export const authContext = createContext<any>('')
